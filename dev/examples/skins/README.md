# Example downstream skins

These files illustrate the version 1 override structure for the three known
task contexts. They are examples, not approved customer copy. Product owners
should review all identity and guidance text before using one in a deployment.

Copy the relevant file to `inst/app/skin.yml` in a downstream repository and
keep only fields that differ from `inst/app/skin-default.yml`.
