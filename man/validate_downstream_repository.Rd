% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_downstream_repository.R
\name{validate_downstream_repository}
\alias{validate_downstream_repository}
\title{Validate a Downstream Application Repository}
\usage{
validate_downstream_repository(file = ".fluvial-app.yml", repository = ".")
}
\arguments{
\item{file}{Path to `.fluvial-app.yml`.}

\item{repository}{Path within the downstream Git repository.}
}
\value{
A machine-readable list of validated metadata and local Git
  evidence, invisibly.
}
\description{
Validate `.fluvial-app.yml` and inspect local Git evidence for a downstream
customer application. The repository must be clean, its configured
`upstream` remote must use the canonical URL, the recorded release tag must
resolve locally and be an ancestor of `HEAD`, and changes since that release
must remain within downstream-owned paths.
}
\details{
This function is read-only. It does not fetch tags, modify Git state,
generate a manifest, run tests, or deploy the application.
}
